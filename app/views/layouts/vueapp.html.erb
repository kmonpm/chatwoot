<!DOCTYPE html>
<html>
  <head>
    <title>Boster Chat</title>

    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no, user-scalable=0"/>

    <% if @global_config['DISPLAY_MANIFEST'] %>
      <meta name="msapplication-TileColor" content="#1D6EF5">
      <meta name="msapplication-TileImage" content="/ms-icon-144x144.png">
      <meta name="theme-color" content="#1D6EF5">
      <meta name="description" content="Boster Chat — customer support made simple.">
      <% if ENV['IOS_APP_IDENTIFIER'].present? %>
        <meta name="apple-itunes-app" content='app-id=<%= ENV['IOS_APP_IDENTIFIER'] %>'>
      <% end %>
      <link rel="apple-touch-icon" sizes="57x57"   href="/apple-icon-57x57.png">
      <link rel="apple-touch-icon" sizes="60x60"   href="/apple-icon-60x60.png">
      <link rel="apple-touch-icon" sizes="72x72"   href="/apple-icon-72x72.png">
      <link rel="apple-touch-icon" sizes="76x76"   href="/apple-icon-76x76.png">
      <link rel="apple-touch-icon" sizes="114x114" href="/apple-icon-114x114.png">
      <link rel="apple-touch-icon" sizes="120x120" href="/apple-icon-120x120.png">
      <link rel="apple-touch-icon" sizes="144x144" href="/apple-icon-144x144.png">
      <link rel="apple-touch-icon" sizes="152x152" href="/apple-icon-152x152.png">
      <link rel="apple-touch-icon" sizes="180x180" href="/apple-icon-180x180.png">
      <link rel="icon" type="image/png" sizes="192x192" href="/android-icon-192x192.png">
      <link class="favicon" rel="icon" type="image/png" sizes="32x32" href="/favicon-32x32.png">
      <link class="favicon" rel="icon" type="image/png" sizes="96x96"  href="/favicon-96x96.png">
      <link class="favicon" rel="icon" type="image/png" sizes="16x16" href="/favicon-16x16.png">
      <link rel="manifest" href="/manifest.json">
    <% end %>

    <link rel="icon" type="image/png" sizes="512x512" href="<%= @global_config['LOGO_THUMBNAIL'] %>">
    <%= csrf_meta_tags %>

    <script nonce="<%= content_security_policy_nonce %>">
      window.chatwootConfig = {
        hostURL:                 '<%= ENV.fetch('FRONTEND_URL', '') %>',
        helpCenterURL:           '<%= ENV.fetch('HELPCENTER_URL', '') %>',
        fbAppId:                 '<%= @global_config['FB_APP_ID'] %>',
        instagramAppId:          '<%= @global_config['INSTAGRAM_APP_ID'] %>',
        tiktokAppId:             '<%= @global_config['TIKTOK_APP_ID'] %>',
        googleOAuthClientId:     '<%= ENV.fetch('GOOGLE_OAUTH_CLIENT_ID', nil) %>',
        googleOAuthCallbackUrl:  '<%= ENV.fetch('GOOGLE_OAUTH_CALLBACK_URL', nil) %>',
        allowedLoginMethods:     <%= @global_config['ALLOWED_LOGIN_METHODS'].to_json.html_safe %>,
        fbApiVersion:            '<%= @global_config['FACEBOOK_API_VERSION'] %>',
        whatsappAppId:           '<%= @global_config['WHATSAPP_APP_ID'] %>',
        whatsappConfigurationId: '<%= @global_config['WHATSAPP_CONFIGURATION_ID'] %>',
        whatsappApiVersion:      '<%= @global_config['WHATSAPP_API_VERSION'] %>',
        signupEnabled:           '<%= @global_config['ENABLE_ACCOUNT_SIGNUP'] %>',
        isEnterprise:            '<%= @global_config['IS_ENTERPRISE'] %>',
        isMfaEnabled:            '<%= Chatwoot.mfa_enabled? %>',
        <% if @global_config['IS_ENTERPRISE'] %>
        enterprisePlanName: '<%= @global_config['INSTALLATION_PRICING_PLAN'] %>',
        <% end %>
        <% if @global_config['VAPID_PUBLIC_KEY'] %>
        vapidPublicKey: new Uint8Array(<%= Base64.urlsafe_decode64(@global_config['VAPID_PUBLIC_KEY']).bytes %>),
        <% end %>
        enabledLanguages: <%= available_locales_with_name.to_json.html_safe %>,
        helpUrls:         <%= feature_help_urls.to_json.html_safe %>,
        selectedLocale:   '<%= I18n.locale %>'
      }
      window.globalConfig       = <%= raw @global_config.to_json %>
      window.browserConfig      = { browser_name: '<%= browser.name %>' }
      window.errorLoggingConfig = '<%= ENV.fetch('SENTRY_FRONTEND_DSN', '') || ENV.fetch('SENTRY_DSN', '') %>'
    </script>

    <% if @global_config['CLOUD_ANALYTICS_TOKEN'].present? %>
      <script nonce="<%= content_security_policy_nonce %>">
        window.analyticsConfig = { token: '<%= @global_config['CLOUD_ANALYTICS_TOKEN'] %>' }
      </script>
    <% end %>

    <%= vite_client_tag %>
    <%= vite_javascript_tag @application_pack %>

    <!-- ORIGINAL WORKING THEME — restored from working version -->
    <style nonce="<%= content_security_policy_nonce %>">

      :root {
        --b-blue:         #1D6EF5;
        --b-blue-dark:    #1450C8;
        --b-blue-navy:    #162F6A;
        --b-blue-light:   #EFF6FF;
        --b-blue-pale:    #F3F7FF;
        --b-blue-border:  #DBEAFE;
        --b-sidebar-icon: #7FB3FF;
        --b-white:        #FFFFFF;
        --b-radius:       10px;
      }

      /* This was working before dark mode changes broke it */
      body, html {
        background: var(--b-blue-pale) !important;
      }

      #app {
        font-family: 'Segoe UI', system-ui, -apple-system, sans-serif !important;
      }

      /* ── SIDEBAR ─────────────────────────────────────────── */
      #app aside.bg-n-background,
      #app aside[class*="bg-n-background"] {
        background: #1a3a7a !important;
        border-right: 1px solid rgba(255,255,255,0.10) !important;
      }

      #app aside.bg-n-background *,
      #app aside[class*="bg-n-background"] * {
        color: #FFFFFF !important;
      }

      #app aside.bg-n-background svg,
      #app aside[class*="bg-n-background"] svg {
        color: #FFFFFF !important;
        stroke: #FFFFFF !important;
        opacity: 0.9;
      }

      #app aside.bg-n-background .active,
      #app aside[class*="bg-n-background"] .active,
      #app aside.bg-n-background [aria-current="page"],
      #app aside[class*="bg-n-background"] [aria-current="page"] {
        background: var(--b-blue) !important;
        border-radius: var(--b-radius) !important;
      }

      #app aside.bg-n-background .active *,
      #app aside[class*="bg-n-background"] .active * {
        color: #FFFFFF !important;
        opacity: 1 !important;
      }

      #app aside.bg-n-background a:hover,
      #app aside[class*="bg-n-background"] a:hover,
      #app aside.bg-n-background button:hover,
      #app aside[class*="bg-n-background"] button:hover {
        background: rgba(255,255,255,0.12) !important;
        border-radius: var(--b-radius) !important;
      }

      /* ── LOGO ───────────────────────────────────────────── */
      #app img.size-4,
      #app div.size-6 img,
      #app div.size-6 > img,
      #app [class*="place-content-center"] img {
        display: none !important;
        visibility: hidden !important;
      }

      #app div.size-6::after,
      #app [class*="place-content-center"][class*="size-6"]::after {
        content: "B";
        display: flex !important;
        align-items: center;
        justify-content: center;
        width: 28px;
        height: 28px;
        background: var(--b-blue);
        color: #fff !important;
        font-size: 15px;
        font-weight: 800;
        border-radius: 7px;
        font-family: 'Segoe UI', system-ui, sans-serif;
        letter-spacing: -0.3px;
        flex-shrink: 0;
      }

      /* ── MAIN CONTENT ───────────────────────────────────── */
      #app main.bg-n-surface-1,
      #app main[class*="bg-n-surface-1"],
      #app [class*="bg-n-surface-1"] {
        background: var(--b-blue-pale) !important;
      }

      #app .flex.flex-col.h-full.bg-n-background,
      #app [class*="conversations-list"] {
        background: #F8FAFF !important;
        border-right: 1px solid var(--b-blue-border) !important;
      }

      /* ── HEADER ─────────────────────────────────────────── */
      #app header,
      #app .view-header,
      #app [class*="view-header"],
      #app [class*="page-header"] {
        background: var(--b-white) !important;
        border-bottom: 1px solid var(--b-blue-border) !important;
        box-shadow: 0 1px 6px rgba(29,110,245,0.06) !important;
      }

      /* ── BUTTONS ────────────────────────────────────────── */
      #app [class*="bg-woot-500"],
      #app [class*="bg-n-brand"],
      #app [class*="bg-brand"],
      #app button[data-testid="submit_button"],
      #app button[type="submit"] {
        background: var(--b-blue) !important;
        border-color: var(--b-blue) !important;
        color: #fff !important;
      }

      #app [class*="bg-woot-500"]:hover,
      #app [class*="bg-n-brand"]:hover,
      #app button[type="submit"]:hover {
        background: var(--b-blue-dark) !important;
        border-color: var(--b-blue-dark) !important;
      }

      #app [class*="border-woot"],
      #app [class*="border-n-brand"],
      #app [class*="border-brand"] {
        border-color: var(--b-blue) !important;
        color: var(--b-blue) !important;
      }

      #app button, #app [role="button"] { border-radius: var(--b-radius) !important; }

      /* ── TEXT / BORDERS ─────────────────────────────────── */
      #app [class*="text-woot"], #app [class*="text-n-brand"], #app [class*="text-brand"] { color: var(--b-blue) !important; }
      #app [class*="border-b-woot"], #app [class*="border-b-brand"], #app [class*="border-b-n-brand"] { border-bottom-color: var(--b-blue) !important; color: var(--b-blue) !important; }

      /* ── INPUTS ─────────────────────────────────────────── */
      #app input:focus, #app textarea:focus, #app select:focus {
        border-color: var(--b-blue) !important;
        box-shadow: 0 0 0 3px rgba(29,110,245,0.10) !important;
        outline: none !important;
      }

      /* ── BADGES ─────────────────────────────────────────── */
      #app [class*="badge"], #app .unread-badge, #app [class*="unread-count"] {
        background: var(--b-blue) !important;
        color: #fff !important;
      }

      /* ── CARDS ──────────────────────────────────────────── */
      #app .bg-white.rounded-lg, #app [class*="bg-white"][class*="rounded"] {
        border: 1px solid var(--b-blue-border) !important;
        box-shadow: 0 2px 12px rgba(29,110,245,0.06) !important;
      }

      /* ── SCROLLBARS ─────────────────────────────────────── */
      #app *::-webkit-scrollbar { width: 5px; height: 5px; }
      #app *::-webkit-scrollbar-track { background: #EEF3FF; }
      #app *::-webkit-scrollbar-thumb { background: #BFDBFE; border-radius: 99px; }
      #app *::-webkit-scrollbar-thumb:hover { background: var(--b-blue); }

      /* ── DARK MODE ──────────────────────────────────────── */
      body.dark #app main.bg-n-surface-1,
      body.dark #app .bg-n-surface-1 { background: #111C2D !important; }
      body.dark #app .conversations-list-wrap { background: #0F1A2B !important; border-right: 1px solid rgba(29,110,245,0.15) !important; }
      body.dark #app header, body.dark #app [class*="view-header"] { background: rgba(10,18,32,0.96) !important; border-bottom: 1px solid rgba(29,110,245,0.18) !important; }

    </style>

    <!-- LOGIN BACKGROUND FIX — exact script provided by user, confirmed working logic -->
    <script nonce="<%= content_security_policy_nonce %>">
    (function() {
      var LIGHT_BG = '#F3F7FF';
      var DARK_BG  = '#0a1628';

      function fixLoginBg() {
        var app = document.getElementById('app');
        if (!app) return;

        var isDark      = document.body.classList.contains('dark');
        var isLogin     = !app.querySelector('aside');
        var isDashboard = !!app.querySelector('aside');

        if (isLogin && app.children.length > 0) {
          app.style.setProperty('background', isDark ? DARK_BG : LIGHT_BG, 'important');
        } else if (isDashboard) {
          app.style.removeProperty('background');
        }
      }

      var appEl = document.getElementById('app');
      if (appEl) {
        new MutationObserver(fixLoginBg).observe(appEl, { childList: true, subtree: true });
      } else {
        var bodyObs = new MutationObserver(function() {
          var app = document.getElementById('app');
          if (app) {
            bodyObs.disconnect();
            new MutationObserver(fixLoginBg).observe(app, { childList: true, subtree: true });
            fixLoginBg();
          }
        });
        bodyObs.observe(document.body, { childList: true });
      }

      new MutationObserver(fixLoginBg).observe(document.body, {
        attributes: true, attributeFilter: ['class']
      });

      setTimeout(fixLoginBg, 0);
      setTimeout(fixLoginBg, 100);
      setTimeout(fixLoginBg, 400);
      setTimeout(fixLoginBg, 1000);
    })();
    </script>

    <!-- WHITE-LABEL + TAB PILLS -->
    <script nonce="<%= content_security_policy_nonce %>">
    (function () {
      var BRAND_OLD       = 'Chatwoot';
      var BRAND_NEW       = 'Boster Chat';
      var BRAND_OLD_LOWER = 'chatwoot';

      function replaceBrandText(text) {
        if (!text) return text;
        return text.replace(/Chatwoot/g, BRAND_NEW).replace(/chatwoot/g, 'bosterchat').replace(/CHATWOOT/g, BRAND_NEW.toUpperCase());
      }

      function replaceTextNodes(root) {
        var walker = document.createTreeWalker(root, NodeFilter.SHOW_TEXT, null, false);
        var node;
        while ((node = walker.nextNode())) {
          if (node.nodeValue && node.nodeValue.toLowerCase().includes(BRAND_OLD_LOWER))
            node.nodeValue = replaceBrandText(node.nodeValue);
        }
      }

      function replaceAttributes(root) {
        root.querySelectorAll('*').forEach(function(el) {
          ['title','alt','placeholder','aria-label','content','value'].forEach(function(attr) {
            var val = el.getAttribute(attr);
            if (val && val.toLowerCase().includes(BRAND_OLD_LOWER))
              el.setAttribute(attr, replaceBrandText(val));
          });
          if ((el.tagName === 'INPUT' || el.tagName === 'TEXTAREA') && el.value && el.value.toLowerCase().includes(BRAND_OLD_LOWER))
            el.value = replaceBrandText(el.value);
        });
      }

      function replaceMetaTags() {
        if (document.title.toLowerCase().includes(BRAND_OLD_LOWER))
          document.title = replaceBrandText(document.title);
        document.querySelectorAll('meta[name="application-name"],meta[property="og:site_name"],meta[name="description"]').forEach(function(m) {
          var c = m.getAttribute('content') || '';
          if (c.toLowerCase().includes(BRAND_OLD_LOWER)) m.setAttribute('content', replaceBrandText(c));
        });
      }

      function applyTabPills() {
        var isDark = document.body.classList.contains('dark');
        document.querySelectorAll('.conversations-list-wrap button, .conversations-list-wrap a, .conversations-list-wrap li').forEach(function(el) {
          var text = ((el.childNodes[0] && el.childNodes[0].textContent) || el.textContent || '').trim();
          if (!/^(Mine|Unassigned|All)\b/.test(text)) return;
          var isActive = el.classList.contains('active') || el.getAttribute('aria-selected') === 'true' || el.classList.contains('is-active');
          el.style.cssText += 'border-radius:20px;padding:3px 14px;font-size:13px;font-weight:500;cursor:pointer;display:inline-flex;align-items:center;white-space:nowrap;transition:background 130ms ease;';
          if (isActive) {
            el.style.background = '#1D6EF5';
            el.style.color = '#FFFFFF';
            el.querySelectorAll('*').forEach(function(c) { c.style.color = '#FFFFFF'; });
          } else {
            el.style.background = 'transparent';
            el.style.color = isDark ? '#94AFCF' : '#475569';
            el.querySelectorAll('*').forEach(function(c) { c.style.color = isDark ? '#94AFCF' : '#475569'; });
          }
        });
      }

      function observeChanges(root) {
        new MutationObserver(function(mutations) {
          var hasNew = false;
          mutations.forEach(function(m) {
            m.addedNodes && m.addedNodes.forEach(function(node) {
              if (node.nodeType === 1) { replaceTextNodes(node); replaceAttributes(node); hasNew = true; }
              else if (node.nodeType === 3 && node.nodeValue && node.nodeValue.toLowerCase().includes(BRAND_OLD_LOWER))
                node.nodeValue = replaceBrandText(node.nodeValue);
            });
            if (m.type === 'characterData' && m.target.nodeValue && m.target.nodeValue.toLowerCase().includes(BRAND_OLD_LOWER))
              m.target.nodeValue = replaceBrandText(m.target.nodeValue);
          });
          if (hasNew) { clearTimeout(window._bTabTimer); window._bTabTimer = setTimeout(applyTabPills, 120); }
        }).observe(root, { childList: true, subtree: true, characterData: true });
      }

      function runAll() {
        var root = document.getElementById('app') || document.body;
        replaceTextNodes(root); replaceAttributes(root); replaceMetaTags(); applyTabPills(); observeChanges(root);
        setTimeout(applyTabPills, 500); setTimeout(applyTabPills, 1500);
      }

      if (document.readyState === 'loading') document.addEventListener('DOMContentLoaded', runAll);
      else runAll();
    })();
    </script>

  </head>

  <body class="text-slate-600">
    <div id="app"></div>
    <noscript id="noscript">This app works best with JavaScript enabled.</noscript>
    <%= yield %>
    <% if @dashboard_scripts.present? %>
      <%= @dashboard_scripts.html_safe %>
    <% end %>
  </body>
</html>
