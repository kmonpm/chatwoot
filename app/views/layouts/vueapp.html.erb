<!DOCTYPE html>
<html>
  <head>
    <title>
      Boster Chat
    </title>

    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no, user-scalable=0"/>

    <% if @global_config['DISPLAY_MANIFEST'] %>
      <meta name="msapplication-TileColor" content="#1f93ff">
      <meta name="msapplication-TileImage" content="/ms-icon-144x144.png">
      <meta name="theme-color" content="#1f93ff">
      <meta name="description" content="<%= @global_config['INSTALLATION_NAME'] %> is a customer support solution that helps companies engage customers over Messenger, Twitter, Telegram, WeChat, Whatsapp. Simply connect your channels and converse with your customers from a single place. Easily add new agents to your system and have them own and resolve conversations with customers. <%= @global_config['INSTALLATION_NAME'] %> also gives you real-time reports to measure your team's performance, canned responses to easily respond to frequently asked questions and private notes for agents to collaborate among themselves.">

      <% if ENV['IOS_APP_IDENTIFIER'].present? %>
        <meta name="apple-itunes-app" content='app-id=<%= ENV['IOS_APP_IDENTIFIER'] %>'>
      <% end %>

      <link rel="apple-touch-icon" sizes="57x57" href="/apple-icon-57x57.png">
      <link rel="apple-touch-icon" sizes="60x60" href="/apple-icon-60x60.png">
      <link rel="apple-touch-icon" sizes="72x72" href="/apple-icon-72x72.png">
      <link rel="apple-touch-icon" sizes="76x76" href="/apple-icon-76x76.png">
      <link rel="apple-touch-icon" sizes="114x114" href="/apple-icon-114x114.png">
      <link rel="apple-touch-icon" sizes="120x120" href="/apple-icon-120x120.png">
      <link rel="apple-touch-icon" sizes="144x144" href="/apple-icon-144x144.png">
      <link rel="apple-touch-icon" sizes="152x152" href="/apple-icon-152x152.png">
      <link rel="apple-touch-icon" sizes="180x180" href="/apple-icon-180x180.png">

      <link rel="icon" type="image/png" sizes="192x192" href="/android-icon-192x192.png">
      <link class="favicon" rel="icon" type="image/png" sizes="32x32" href="/favicon-32x32.png">
      <link class="favicon" rel="icon" type="image/png" sizes="96x96" href="/favicon-96x96.png">
      <link class="favicon" rel="icon" type="image/png" sizes="16x16" href="/favicon-16x16.png">

      <link rel="manifest" href="/manifest.json">
    <% end %>

    <!-- Use installation logo thumbnail as favicon -->
    <link rel="icon" type="image/png" sizes="512x512" href="<%= @global_config['LOGO_THUMBNAIL'] %>">

    <%= csrf_meta_tags %>

    <script nonce="<%= content_security_policy_nonce %>">
      window.chatwootConfig = {
        hostURL: '<%= ENV.fetch('FRONTEND_URL', '') %>',
        helpCenterURL: '<%= ENV.fetch('HELPCENTER_URL', '') %>',
        fbAppId: '<%= @global_config['FB_APP_ID'] %>',
        instagramAppId: '<%= @global_config['INSTAGRAM_APP_ID'] %>',
        tiktokAppId: '<%= @global_config['TIKTOK_APP_ID'] %>',
        googleOAuthClientId: '<%= ENV.fetch('GOOGLE_OAUTH_CLIENT_ID', nil) %>',
        googleOAuthCallbackUrl: '<%= ENV.fetch('GOOGLE_OAUTH_CALLBACK_URL', nil) %>',
        allowedLoginMethods: <%= @global_config['ALLOWED_LOGIN_METHODS'].to_json.html_safe %>,
        fbApiVersion: '<%= @global_config['FACEBOOK_API_VERSION'] %>',
        whatsappAppId: '<%= @global_config['WHATSAPP_APP_ID'] %>',
        whatsappConfigurationId: '<%= @global_config['WHATSAPP_CONFIGURATION_ID'] %>',
        whatsappApiVersion: '<%= @global_config['WHATSAPP_API_VERSION'] %>',
        signupEnabled: '<%= @global_config['ENABLE_ACCOUNT_SIGNUP'] %>',
        isEnterprise: '<%= @global_config['IS_ENTERPRISE'] %>',
        isMfaEnabled: '<%= Chatwoot.mfa_enabled? %>',
        <% if @global_config['IS_ENTERPRISE'] %>
        enterprisePlanName: '<%= @global_config['INSTALLATION_PRICING_PLAN'] %>',
        <% end %>
        <% if @global_config['VAPID_PUBLIC_KEY'] %>
        vapidPublicKey: new Uint8Array(<%= Base64.urlsafe_decode64(@global_config['VAPID_PUBLIC_KEY']).bytes %>),
        <% end %>
        enabledLanguages: <%= available_locales_with_name.to_json.html_safe %>,
        helpUrls: <%= feature_help_urls.to_json.html_safe %>,
        selectedLocale: '<%= I18n.locale %>'
      }
      window.globalConfig = <%= raw @global_config.to_json %>
      window.browserConfig = { browser_name: '<%= browser.name %>' }
      window.errorLoggingConfig = '<%= ENV.fetch('SENTRY_FRONTEND_DSN', '') || ENV.fetch('SENTRY_DSN', '') %>'
    </script>

    <% if @global_config['CLOUD_ANALYTICS_TOKEN'].present? %>
      <script nonce="<%= content_security_policy_nonce %>">
        window.analyticsConfig = { token: '<%= @global_config['CLOUD_ANALYTICS_TOKEN'] %>' }
      </script>
    <% end %>

    <%= vite_client_tag %>
    <%= vite_javascript_tag @application_pack %>

    <!-- ✅ Boster Chat — CSP-safe branding overrides -->
    <style nonce="<%= content_security_policy_nonce %>">
      /* Light modern background */
      body {
        background:
          radial-gradient(900px 420px at 15% 0%, rgba(37,99,235,.18), transparent 60%),
          radial-gradient(700px 380px at 85% 10%, rgba(14,165,233,.14), transparent 55%),
          #f6f8fc !important;
      }

      /* Login card polish */
      #app .bg-white,
      #app [class*="bg-white"] {
        border-radius: 16px !important;
        box-shadow: 0 18px 45px rgba(2,6,23,0.10) !important;
      }

      /* Buttons */
      #app button,
      #app [role="button"],
      #app .btn {
        border-radius: 12px !important;
      }

      /*
       * ── LOGO HIDE — precise selectors from DevTools ──────────────────────
       * Chatwoot renders two <img> tags inside a <section class="max-w-5xl mx-auto">:
       *   Light mode: img.block.w-auto.h-8.mx-auto.dark:hidden  src="/brand-assets/logo.svg"
       *   Dark  mode: img.hidden.w-auto.h-8.mx-auto.dark:block  src="/brand-assets/logo_dark.svg"
       * We hide both using the src path — the most stable selector possible.
       */
      #app img[src="/brand-assets/logo.svg"],
      #app img[src="/brand-assets/logo_dark.svg"],
      #app img[src*="/brand-assets/logo"],
      #app img[src*="brand-assets"],
      #app img[alt*="chatwoot" i],
      #app img[alt*="boster" i] {
        display: none !important;
        visibility: hidden !important;
        opacity: 0 !important;
        pointer-events: none !important;
      }

      /*
       * Also hide the parent <section> logo wrapper entirely
       * It has class="max-w-5xl mx-auto" and contains only the logo + h2
       * We target it via its direct img children with brand-asset srcs
       */
      #app section:has(img[src*="brand-assets"]) {
        /* Hide only the logo image row, keep the h2 title visible */
      }
      #app section:has(img[src*="brand-assets"]) img {
        display: none !important;
      }

      /* Buttons */
      #app button,
      #app [role="button"],
      #app .btn {
        border-radius: 12px !important;
      }
    </style>

    <!-- ✅ Boster Chat — robust white-label JS (MutationObserver + deep text nodes) -->
    <script nonce="<%= content_security_policy_nonce %>">
    (function() {
      const BRAND_OLD       = 'Chatwoot';
      const BRAND_NEW       = 'Boster Chat';
      const BRAND_OLD_LOWER = 'chatwoot';

      // ── Util: replace all case variants ──────────────────────────────────
      function replaceBrandText(text) {
        if (!text) return text;
        return text
          .replace(/Chatwoot/g, BRAND_NEW)
          .replace(/chatwoot/g, 'bosterchat')
          .replace(/CHATWOOT/g, BRAND_NEW.toUpperCase());
      }

      // ── 1) Deep text-node walk (catches all Vue-rendered strings) ────────
      function replaceTextNodes(root) {
        const walker = document.createTreeWalker(
          root,
          NodeFilter.SHOW_TEXT,
          null,
          false
        );
        let node;
        while ((node = walker.nextNode())) {
          if (node.nodeValue && node.nodeValue.toLowerCase().includes(BRAND_OLD_LOWER)) {
            node.nodeValue = replaceBrandText(node.nodeValue);
          }
        }
      }

      // ── 2) Attribute replacement (title, alt, placeholder, aria-label…) ──
      function replaceAttributes(root) {
        const attrs = ['title', 'alt', 'placeholder', 'aria-label', 'content', 'value'];
        root.querySelectorAll('*').forEach(el => {
          attrs.forEach(attr => {
            const val = el.getAttribute(attr);
            if (val && val.toLowerCase().includes(BRAND_OLD_LOWER)) {
              el.setAttribute(attr, replaceBrandText(val));
            }
          });
          // Input / textarea live values
          if ((el.tagName === 'INPUT' || el.tagName === 'TEXTAREA') &&
              el.value && el.value.toLowerCase().includes(BRAND_OLD_LOWER)) {
            el.value = replaceBrandText(el.value);
          }
        });
      }

      // ── 3) Hide logos — targets both "chatwoot" text AND /brand-assets/ path
      function hideLogos(root) {
        root.querySelectorAll('img, svg, picture, source').forEach(el => {
          const alt    = (el.getAttribute('alt')   || '').toLowerCase();
          const src    = (el.getAttribute('src')   || '').toLowerCase();
          const srcset = (el.getAttribute('srcset')|| '').toLowerCase();
          const dtitle = (el.getAttribute('data-original-title') || '').toLowerCase();

          const isBrandAsset = src.includes('/brand-assets/') || srcset.includes('/brand-assets/');
          const isChatwoot   = [alt, src, srcset, dtitle].some(v => v.includes(BRAND_OLD_LOWER));

          if (isBrandAsset || isChatwoot) {
            el.style.setProperty('display',        'none',   'important');
            el.style.setProperty('visibility',     'hidden', 'important');
            el.style.setProperty('opacity',        '0',      'important');
            el.style.setProperty('pointer-events', 'none',   'important');
          }
        });
      }

      // ── 4) Hide brand containers only if they directly reference Chatwoot ─
      function hideBrandContainers(root) {
        const selectors = [
          'header', 'nav',
          '[class*="logo"]', '[class*="brand"]',
          '[class*="Logo"]', '[class*="Brand"]',
          '[id*="logo"]',    '[id*="brand"]'
        ];
        root.querySelectorAll(selectors.join(',')).forEach(el => {
          const ownText = Array.from(el.childNodes)
            .filter(n => n.nodeType === Node.TEXT_NODE)
            .map(n => n.nodeValue || '')
            .join('').toLowerCase();
          const imgAlt = (el.querySelector('img')?.getAttribute('alt') || '').toLowerCase();
          const imgSrc = (el.querySelector('img')?.getAttribute('src') || '').toLowerCase();
          if (
            ownText.includes(BRAND_OLD_LOWER) ||
            imgAlt.includes(BRAND_OLD_LOWER)  ||
            imgSrc.includes(BRAND_OLD_LOWER)
          ) {
            el.style.setProperty('display', 'none', 'important');
          }
        });
      }

      // ── 5) Page <title> + meta tags ──────────────────────────────────────
      function replaceMetaTags() {
        if (document.title.toLowerCase().includes(BRAND_OLD_LOWER)) {
          document.title = replaceBrandText(document.title);
        }
        document.querySelectorAll(
          'meta[name="application-name"], meta[property="og:site_name"], meta[name="description"]'
        ).forEach(meta => {
          const content = meta.getAttribute('content') || '';
          if (content.toLowerCase().includes(BRAND_OLD_LOWER)) {
            meta.setAttribute('content', replaceBrandText(content));
          }
        });
      }

      // ── 6) MutationObserver — survives Vue/React re-renders ──────────────
      function observeChanges(root) {
        const observer = new MutationObserver(mutations => {
          for (const mutation of mutations) {
            if (mutation.type === 'childList') {
              mutation.addedNodes.forEach(node => {
                if (node.nodeType === Node.ELEMENT_NODE) {
                  replaceTextNodes(node);
                  replaceAttributes(node);
                  hideLogos(node);
                } else if (node.nodeType === Node.TEXT_NODE) {
                  if (node.nodeValue && node.nodeValue.toLowerCase().includes(BRAND_OLD_LOWER)) {
                    node.nodeValue = replaceBrandText(node.nodeValue);
                  }
                }
              });
            } else if (mutation.type === 'characterData') {
              if (mutation.target.nodeValue &&
                  mutation.target.nodeValue.toLowerCase().includes(BRAND_OLD_LOWER)) {
                mutation.target.nodeValue = replaceBrandText(mutation.target.nodeValue);
              }
            }
          }
        });

        observer.observe(root, {
          childList:     true,
          subtree:       true,
          characterData: true
        });
      }

      // ── Run everything ───────────────────────────────────────────────────
      function runAll() {
        const root = document.getElementById('app') || document.body;
        replaceTextNodes(root);
        replaceAttributes(root);
        hideLogos(root);
        hideBrandContainers(root);
        replaceMetaTags();
        observeChanges(root);  // keeps watching for Vue dynamic updates
      }

      // Fire immediately if DOM is ready, otherwise wait
      if (document.readyState === 'loading') {
        document.addEventListener('DOMContentLoaded', runAll);
      } else {
        runAll();
      }
    })();
    </script>
  </head>

  <body class="text-slate-600">
    <div id="app"></div>
    <noscript id="noscript">This app works best with JavaScript enabled.</noscript>
    <%= yield %>
    <% if @dashboard_scripts.present? %>
      <%= @dashboard_scripts.html_safe %>
    <% end %>
  </body>
</html>
